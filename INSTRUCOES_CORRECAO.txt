CORREÇÃO DO ERRO 500 E DO LINK DO PACIENTE

CAUSA CONFIRMADA NO LOG DA VERCEL:
A rota antiga api/clientes/index.ts tentava importar /lib/firestore, mas esse módulo não estava no bundle da função.

ESTA VERSÃO USA APENAS:
api/[...path].ts

PASSOS OBRIGATÓRIOS:
1. Copie todo o conteúdo desta pasta para a raiz do repositório Denise-App- e substitua os arquivos.
2. Na raiz do repositório, execute APLICAR_CORRECAO.bat.
3. Confirme que as pastas api/clientes e lib foram EXCLUÍDAS. Não basta substituir arquivos: elas precisam desaparecer.
4. Abra o GitHub Desktop.
5. O GitHub Desktop deve mostrar exclusões em api/clientes e lib, além das alterações desta correção.
6. Commit: Corrigir rota 500 e link do paciente
7. Push origin.
8. Aguarde o deploy novo ficar Ready na Vercel.
9. Cadastre um paciente novo e abra o link em janela anônima.

ALTERAÇÕES:
- remove conflito de rotas serverless;
- mantém uma única função de API;
- confirma no Firestore que o token pode ser lido antes de retornar sucesso;
- botão mostra Salvando... e bloqueia clique duplicado;
- apresenta a mensagem real em caso de falha;
- respostas da API sem cache.

VALIDAÇÃO LOCAL:
- npm run lint: aprovado
- npm run build: aprovado
